============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install complexitygraph
    $ pip install complexitygraph

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv complexitygraph
    $ pip install complexitygraph
